## Pseudocode 1

START
Declare studentName as string
Declare rollNumber as integer
Declare age as integer
Declare height as float
Declare GPA as float
Declare section as character
Input studentName, rollNumber, age, height, GPA, and section
Display all student information in an organized format
END

## Pseducode 2

START
Declare character variable ch
Ask the user to enter a character
Read ch using getchar()
Display ch using putchar()
END

## Pseudocde 3

START
Declare a floating-point variable value
Input value from the user
Display value using default precision
Display value with 2 digits after the decimal point
Display value with 4 digits after the decimal point
Display value with 6 digits after the decimal point
END
